# Итерация 00002

Дата: 2026-04-16

## Что сделано

- Добавлен DI через Dishka в API:
  - `kov/di/providers.py` (AppConfig, AsyncEngine/AsyncSession, Qdrant, S3).
  - `apps/api/main.py` переведён на `make_async_container` + `setup_dishka` + корректный lifespan.
  - Для роутов `rag_scan`, `rag_search`, `llm` включён `DishkaRoute` и инъекция через `FromDishka`.
- Исправлены e2e проблемы (multipart dependency, healthchecks, тестовый PDF).
- Расширен функционал сценариев:
  - авто-создание расписаний напоминаний при `POST /users/ensure` (18:00 mood, 21:00 habits);
  - /reports: добавлены monthly отчёты для mood и habits;
  - /techniques: добавлена история предложений и простая анти-повторяемость, плюс `POST /techniques/done`;
  - /scenarios/interview/answer: upsert ответов (редактирование);
  - /export/{user_id}: экспорт основных пользовательских данных (интервью/настроение/привычки/диалоги).
- Scheduler: учёт timezone пользователя при проверке расписаний.

## Тесты

- Unit: `tests/unit/*` — OK.
- E2E: `tests/e2e/test_e2e_rag.py` — OK (поднимается Postgres+Qdrant+S3+API).

